# SmartBiz production database owner credentials.
# Copy to infra/.env.production.owner, chmod 600, and never commit it.
# This identity is used only by PostgreSQL initialization, role bootstrap,
# migrations, and destructive database restore.

DB_OWNER_DATABASE=smartbiz
DB_OWNER_USERNAME=smartbiz
DB_OWNER_PASSWORD=
