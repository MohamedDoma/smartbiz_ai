# SmartBiz production backup credentials.
# Copy to infra/.env.production.backup, chmod 600, and never commit it.
# This role is read-only and isolated from web, worker, and app scheduler containers.

DB_BACKUP_DATABASE=smartbiz
DB_BACKUP_USERNAME=smartbiz_backup
DB_BACKUP_PASSWORD=
